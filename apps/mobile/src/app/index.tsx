// Écran d'entrée de l'app = splash animé. La navigation vers login/tabs est
// gérée par le splash lui-même une fois l'intro terminée et la session résolue.
export { default } from './(auth)/splash';
